{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (uv run)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/.venv/bin/python",
            "args": [],
            "justMyCode": true
        }
    ]
}